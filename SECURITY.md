# 安全說明

## 目前安全邊界

本專案是黑客松原型，**不是已完成身分驗證的帳戶系統**。一般帳號目前依使用者提供的 Email 篩選資料，未驗證信箱所有權；知道 Email 的人可能冒用身分。Job ID 也不是完整授權機制。免額度名單不應被視為安全存取控制。

正式開放敏感用途前，必須加入 Email OTP 或 magic link、伺服器端 session，讓所有帳號 API 與工作讀取以已驗證身分授權，並補上跨帳號拒絕測試。僅隱藏前端按鈕或比對傳入 Email 不足以修復。

部分上游錯誤原文會經密鑰遮罩後回傳或記錄；**遮罩不等於完整個資去識別化**。第三方回應應進一步最小化，診斷資料必須有存取與保留期限管理。

## 憑證

服務密鑰只放 Worker secrets 或忽略版控的環境檔。診斷 API 另需 `DIAG_TOKEN`；D1 ID、帳號 ID 與模型名稱屬識別或設定資訊，不是私鑰。

本機開發追蹤也可能保留帶有 token 的外部請求。請勿分享 `.wrangler/`、`work/`、terminal dump 或完整診斷回應。若憑證曾暴露，應在服務商端撤銷並更新；從檔案刪除不會使舊憑證失效。

## 回報

請透過與維護者已建立的私密管道提供受影響版本、合成資料的重現步驟與影響範圍。不要在一般 Issue、PR、截圖或公開日誌貼密鑰、真實用戶 Email 或資料。

尚未建立正式安全回報 SLA。請先取得授權再進行正式站測試，不得讀取其他使用者資料或觸發真實寄信。
